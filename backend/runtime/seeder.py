"""Startup seeder — placeholder. Tool seeding now happens via the /mcp/servers/{id}/sync endpoint."""


async def seed_tools_and_templates():
    """No-op: built-in tools were removed in favour of MCP-backed tools."""
    pass
